<Page x:Class="XvTHydrospanner.Views.RemoteModsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Remote Mod Library"
      Background="#1E1E1E">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Remote Mod Library - Download Packages" FontSize="24" FontWeight="Bold" 
                   Foreground="White" Margin="0,0,0,20"/>
        
        <!-- Repository Info -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Repository: " Foreground="#999999" VerticalAlignment="Center"/>
            <TextBlock x:Name="RepositoryText" Text="Loading..." Foreground="White" VerticalAlignment="Center"/>
            <Button x:Name="RefreshButton" Content="Refresh" Width="100" Height="30" Margin="20,0,0,0"
                    Style="{StaticResource PrimaryButtonStyle}" Click="RefreshButton_Click"/>
        </StackPanel>
        
        <!-- Search -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <TextBox x:Name="SearchBox" Height="30"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="#333333"/>
                        <Setter Property="BorderBrush" Value="#555555"/>
                        <Setter Property="Padding" Value="5"/>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>
        
        <!-- Packages Grid -->
        <DataGrid x:Name="RemotePackagesGrid" Grid.Row="3" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Background="#252526"
                  Foreground="White"
                  BorderBrush="#3F3F46"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#3F3F46">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Download Package" Click="ContextMenu_Download_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Package Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="120"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="Files" Binding="{Binding FileIds.Count}" Width="60"/>
                <DataGridCheckBoxColumn Header="Downloaded" Binding="{Binding IsDownloaded, Mode=OneWay}" Width="100" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Actions" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Click="DownloadButton_Click"
                                        Tag="{Binding}"
                                        Height="28" Width="28"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        ToolTip="Download Package"
                                        Cursor="Hand"
                                        IsEnabled="{Binding IsDownloaded, Converter={StaticResource InverseBoolConverter}}">
                                    <TextBlock Text="⬇" FontSize="18" Foreground="#2196F3"/>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#333333"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.3"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#252526"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2A2D2E"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#094771"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderBrush" Value="#3F3F46"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
        
        <!-- Status Bar -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusText" Grid.Column="0" Text="Ready" 
                       Foreground="#999999" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="DownloadAllButton" Content="Download All Available Packages" Width="220" Height="35"
                        Style="{StaticResource PrimaryButtonStyle}" Click="DownloadAllButton_Click" Margin="0,0,10,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
