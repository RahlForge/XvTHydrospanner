<Page x:Class="XvTHydrospanner.Views.RemoteModsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Remote Mod Library"
      Background="#1E1E1E">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Remote Mod Library" FontSize="24" FontWeight="Bold" 
                   Foreground="White" Margin="0,0,0,20"/>
        
        <!-- Repository Info -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Repository: " Foreground="#999999" VerticalAlignment="Center"/>
            <TextBlock x:Name="RepositoryText" Text="Loading..." Foreground="White" VerticalAlignment="Center"/>
            <Button x:Name="RefreshButton" Content="Refresh" Width="100" Height="30" Margin="20,0,0,0"
                    Style="{StaticResource PrimaryButtonStyle}" Click="RefreshButton_Click"/>
        </StackPanel>
        
        <!-- Search and Filter -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="SearchBox" Grid.Column="0" Height="30" Margin="0,0,10,0"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="#333333"/>
                        <Setter Property="BorderBrush" Value="#555555"/>
                        <Setter Property="Padding" Value="5"/>
                    </Style>
                </TextBox.Style>
            </TextBox>
            
            <TextBlock Grid.Column="1" Text="Filter:" Foreground="White" Margin="0,0,10,0" VerticalAlignment="Center"/>
            
            <ComboBox x:Name="CategoryFilter" Grid.Column="2" Height="30"
                      SelectionChanged="CategoryFilter_SelectionChanged">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="Background" Value="#333333"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </ComboBox.Style>
            </ComboBox>
        </Grid>
        
        <!-- Files Grid -->
        <DataGrid x:Name="RemoteFilesGrid" Grid.Row="3" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Background="#252526"
                  Foreground="White"
                  BorderBrush="#3F3F46"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#3F3F46">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="150"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="120"/>
                <DataGridTextColumn Header="Size" Binding="{Binding FileSizeBytes, Converter={StaticResource FileSizeConverter}}" Width="80"/>
                <DataGridCheckBoxColumn Header="Downloaded" Binding="{Binding IsDownloaded, Mode=OneWay}" Width="100" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Download" Width="90" Height="25" Margin="5,0"
                                        Click="DownloadButton_Click"
                                        Tag="{Binding}"
                                        IsEnabled="{Binding IsDownloaded, Converter={StaticResource InverseBoolConverter}}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                            <Setter Property="FontSize" Value="11"/>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#252526"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2A2D2E"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#094771"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderBrush" Value="#3F3F46"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
        
        <!-- Status Bar -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusText" Grid.Column="0" Text="Ready" 
                       Foreground="#999999" VerticalAlignment="Center"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="DownloadAllButton" Content="Download All Available" Width="180" Height="35"
                        Style="{StaticResource PrimaryButtonStyle}" Click="DownloadAllButton_Click" Margin="0,0,10,0"/>
                <Button x:Name="ViewPackagesButton" Content="View Packages" Width="150" Height="35"
                        Click="ViewPackagesButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
