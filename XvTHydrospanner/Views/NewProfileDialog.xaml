<Window x:Class="XvTHydrospanner.Views.NewProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Profile" Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#252526">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Profile Name" Foreground="White" Margin="0,0,0,5"/>
        <TextBox x:Name="ProfileNameTextBox" Grid.Row="1" Height="30" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="2" Text="Description (Optional)" Foreground="White" Margin="0,0,0,5"/>
        <TextBox x:Name="ProfileDescriptionTextBox" Grid.Row="3" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Create" Width="80" Height="30" IsDefault="True" 
                    Style="{StaticResource PrimaryButtonStyle}" Click="CreateButton_Click" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="80" Height="30" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
