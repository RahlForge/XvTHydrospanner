<Page x:Class="XvTHydrospanner.Views.WarehousePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#1E1E1E">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Mod Warehouse" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Manage your repository of mod files" FontSize="12" Foreground="#999999" Margin="0,5,0,0"/>
        </StackPanel>
        
        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="AddFileButton" Content="+ Add File" Height="35" Width="120" 
                    Style="{StaticResource PrimaryButtonStyle}" Click="AddFileButton_Click"/>
            <Button x:Name="UploadToRemoteButton" Content="⬆ Upload to Remote" Height="35" Width="150" Margin="10,0,0,0"
                    Click="UploadToRemoteButton_Click"/>
            <TextBox x:Name="SearchBox" Width="250" Height="35" Margin="20,0,0,0" 
                     VerticalContentAlignment="Center" TextChanged="SearchBox_TextChanged"/>
            <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#666666"/>
        </StackPanel>
        
        <!-- Files Grid -->
        <DataGrid x:Name="WarehouseDataGrid" Grid.Row="2" AutoGenerateColumns="False" 
                  Background="#252526" Foreground="#CCCCCC" BorderBrush="#3F3F46" 
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowBackground="#252526" AlternatingRowBackground="#2D2D30"
                  CanUserAddRows="False" IsReadOnly="True" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="Mod" Binding="{Binding ModPackageName}" Width="150" CanUserSort="True"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Target Path" Binding="{Binding TargetRelativePath}" Width="250"/>
                <DataGridTextColumn Header="File Size" Binding="{Binding FileSizeBytes}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Click="DeleteFileButton_Click" 
                                    Tag="{Binding}" Height="25" Width="70"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
